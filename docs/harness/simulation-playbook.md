# Simulation Playbook: concurrent-intent-update

## Agents Involved
| Agent | Role | Fork Point |
|-------|------|-----------|
| agent-A | creates new intent | CRDT fork from main |
| agent-B | modifies same intent's status | CRDT fork from main |
| Human | Reviews and approves | N/A |

## Timeline

```
T=0:   Both agents fork the CRDT document
T=1:   agent-A — create intent `hief-123` with title "Add docs command"
T=2:   agent-B — update status of intent `hief-123` to `in_progress`
T=3:   agent-A — add description field to same intent
T=4:   agent-B — add a child-edge from `hief-123` to `hief-456`
T=5:   CRDT merge: agent-A ← agent-B
T=6:   Verify: merged doc contains both status `in_progress` and description
T=7:   human — review merged graph, resolve any conflicts
T=8:   Verify: human approval recorded in merged doc
```

## Invariants to Verify After Merge
- [ ] Both agents' intents exist in merged document
- [ ] Edge relationships are preserved
- [ ] Status updates from both agents are reflected
- [ ] Actor IDs are distinct
- [ ] Change count increases monotonically
- [ ] No fields lost during merge
- [ ] CRDT tombstones cleaned after merge

---

*Generated by `hief docs generate playbook`. Review and customize before approving.*