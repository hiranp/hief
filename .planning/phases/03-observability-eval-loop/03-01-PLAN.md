---
phase: 03-observability-eval-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db.rs
  - src/errors.rs
  - tests/test_tool_events.rs
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - Tool invocation telemetry is persistently stored.
    - Session-level summaries are queryable without external systems.
    - Migration path is backward-compatible with existing databases.
  artifacts:
    - path: src/db.rs
      provides: tool_events table and session_summary view
    - path: tests/test_tool_events.rs
      provides: migration and query regression tests
  key_links:
    - from: src/db.rs
      to: src/mcp/tools.rs
      via: event write API
      pattern: record_tool_event
---

# Plan 03-01: Telemetry Schema and Write Path

## Objective

Add SQL-native observability primitives that can be consumed by health and eval workflows.

Purpose: Eliminate observability blindness with minimal dependency overhead.
Output: tool_events schema, session_summary view, and tested DB migration path.

## Context

- @.planning/research/STACK.md
- @.planning/research/PITFALLS.md
- @src/db.rs

## Tasks

### Task 1: Add telemetry schema migration for OBS-01

Files: src/db.rs, tests/test_tool_events.rs

Type: auto

TDD: true

Behavior:

- Migration creates tool_events table with required columns.
- Migration creates session_summary view.
- Existing migration sequence remains valid.

Action:

- Add new migration in src/db.rs creating tool_events table and session_summary view per OBS-01.
- Include fields: session_id, tool, query, strategy, result_count, latency_ms, groundedness_score, created_at.
- Add tests to ensure migration order and schema presence.

Verify:

- automated: cargo test test_tool_events -- --nocapture

Done:

- Telemetry schema is present and migration-safe.

### Task 2: Add record_tool_event DB API

Files: src/db.rs, src/errors.rs, tests/test_tool_events.rs

Type: auto

TDD: true

Behavior:

- Event insert succeeds with full payload.
- Optional fields handle null/empty safely.
- Insert/read roundtrip preserves key metrics.

Action:

- Implement typed record_tool_event write helper in src/db.rs and add error mapping where needed in src/errors.rs.
- Add roundtrip tests for insert and session_summary aggregation.

Verify:

- automated: cargo test test_tool_events -- --nocapture

Done:

- Event write API exists and is validated by integration tests.

## Threat Model

Trust boundaries:

- MCP handlers to telemetry DB: Untrusted query strings can poison logs without normalization.

STRIDE register:

- T-03-01, Tampering, tool_events payload, mitigate: Use parameterized SQL inserts and bounded field lengths.
- T-03-02, Repudiation, telemetry traceability, mitigate: Store immutable created_at and tool and session identifiers.
- T-03-SC, Tampering, cargo dependency changes, accept: No package-manager installs in this plan.

## Verification

- cargo test test_tool_events -- --nocapture passes

## Success Criteria

- Telemetry rows and session summaries are queryable in local hief.db.

## Output

Create .planning/phases/03-observability-eval-loop/03-01-SUMMARY.md when done.
