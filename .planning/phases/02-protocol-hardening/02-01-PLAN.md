---
phase: 02-protocol-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp/tools.rs
  - src/errors.rs
  - src/mcp/mod.rs
  - tests/test_mcp_validation.rs
autonomous: true
requirements: [PRO-01, PRO-02]
must_haves:
  truths:
    - Invalid MCP tool params fail with stable, typed errors.
    - Recoverable validation failures include correction hints.
    - Tool handlers preserve backward-compatible success payloads.
  artifacts:
    - path: src/mcp/tools.rs
      provides: validation boundary and hint payload generation
    - path: src/errors.rs
      provides: explicit tool-validation error variants
    - path: tests/test_mcp_validation.rs
      provides: contract regression tests for failures and hints
  key_links:
    - from: src/mcp/tools.rs
      to: src/errors.rs
      via: tool error mapping
      pattern: invalid_params|SecurityViolation
---

# Plan 02-01: Strict MCP Validation and Retry Hints

## Objective

Strengthen MCP contract handling so agent retries become deterministic and low-friction.

Purpose: Reduce tool hallucination and non-adherence failure modes.
Output: Typed validation errors and correction-hint payloads.

## Execution Context

- @~/.copilot/get-shit-done/workflows/execute-plan.md
- @~/.copilot/get-shit-done/templates/summary.md

## Context

- @.planning/research/PITFALLS.md
- @.planning/research/FEATURES.md
- @src/mcp/tools.rs
- @src/errors.rs

## Tasks

### Task 1: Validation boundary for PRO-01

Files: src/mcp/tools.rs, src/errors.rs, tests/test_mcp_validation.rs

Type: auto

TDD: true

Behavior:

- Invalid top_k above bound returns typed validation error.
- Invalid file path with traversal markers returns security error.
- Missing required parameter returns predictable invalid_params shape.

Action:

- Implement strict pre-handler validation in src/mcp/tools.rs for common parameter classes and map failures to explicit error variants in src/errors.rs per PRO-01.
- Ensure error responses preserve machine-readable fields and avoid lossy string-only failures.
- Add tests in tests/test_mcp_validation.rs covering invalid numeric bounds, missing required fields, and path traversal attempts.

Verify:

- automated: cargo test test_mcp_validation -- --nocapture

Done:

- Invalid requests fail consistently at boundary with stable error classes.

### Task 2: Retry hint payloads for PRO-02

Files: src/mcp/tools.rs, src/mcp/mod.rs, tests/test_mcp_validation.rs

Type: auto

TDD: true

Behavior:

- Recoverable validation errors include correction_hint content.
- Non-recoverable errors do not leak unsafe internals.
- Existing successful responses remain unchanged.

Action:

- Add correction-hint payload structure in tool error returns for recoverable failures per PRO-02, including suggested parameter bounds or required field names.
- Keep hint generation deterministic and safe; do not echo sensitive path data.
- Extend tests to assert hint presence and expected shape.

Verify:

- automated: cargo test test_mcp_validation -- --nocapture

Done:

- Recoverable failures provide actionable machine-readable hints with no API regression on success paths.

## Threat Model

Trust boundaries:

- agent input to MCP validation: Untrusted arguments can trigger misuse or traversal attempts.
- validation path to error responses: Error payloads can disclose internal details if uncontrolled.

STRIDE register:

- T-02-01, Tampering, tool parameter parsing, mitigate: Centralize validation and reject malformed inputs before execution.
- T-02-02, Information disclosure, error payloads, mitigate: Return bounded correction hints with no sensitive internals.
- T-02-SC, Tampering, cargo dependency changes, accept: No package-manager installs in this plan.

## Verification

- cargo test test_mcp_validation -- --nocapture passes
- cargo test --test test_config -- --nocapture remains green

## Success Criteria

- MCP handlers fail safely and guide retries with correction hints.

## Output

Create .planning/phases/02-protocol-hardening/02-01-SUMMARY.md when done.
